[build]
  base = "speakup_web"
  publish = "build/web"
  command = "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter pub get && flutter build web --release"

[build.environment]
  FLUTTER_VERSION = "stable"